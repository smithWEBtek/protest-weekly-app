<h1>Event</h1>

<!--this view should list event info. for one event and opportunity to sign up,indicate if ride is needed or can drive in car pool. -->

<p id="notice"><%= notice %></p>

<div class="center">
<tbody>
		<tr>
			<td><strong>Name</strong></td>
			<td><strong>Cause</strong></td>
			<td><strong>Location</strong></td>
			<td><strong>Organization</strong></td>	
			<td><strong>Date & Time</strong></td>
				
		</tr>
		<% @events.each do |event| %>
		<tr>
		<td><%= event.name %></td>
		<td><%= event.cause %></td>
		<td><%= event.location %></td>
		<td><%= organization.name %></td>
		<td><%= event_users.datetime %></td>
	</tr>
	<% end %> 
		
		<%= link_to "Car Pool" %>
		
	<%= link_to "Back", events_path, class: "btn btn-primary btn-light" %></p>	