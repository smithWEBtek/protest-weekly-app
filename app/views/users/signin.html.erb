<%= form_for @user, url: "/users/login" do |f| %>

	
		<%= f.label :name, "Select your name" %><br>
		<%= f.collection_select(:user_id, User.all, :id, :name, {include_blank: "Please select your name"})%>
	

	<!--signin here or with FB -->
 	
		<%= f.submit "Sign In" %>
	
	<% end %>

